import 'package:flutter/material.dart';

class AddAlarmView extends StatelessWidget {
  const AddAlarmView({required Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
